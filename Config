# Please keep all build-tools, dependencies, etc sorted alphabetically.
package.CredentialsFetcherV2 = {
    interfaces = (1.0);

    build-system = bgo-wrap-make;

    deploy = {
        map = (default, "-gopath/**");
    };

    build-tools = {
        1.0 = {
            BrazilMakeGo = 3.0;
            GoLang = 1.x;
            Go3p-Github-Securego-Gosec-V2 = 2.x;
            Go3p-Github-Golangci-GolangciLint-V2 = 2.x;
            Kerberos = 1.x;
        };
    };

    dependencies = {
        1.0 = {
            # Needed for emitting EMF metrics.
            GoAmzn-Metrics = 1.0;
            # Needed for CGO Kerberos integration (provides krb5.h and libkrb5)
            Kerberos = 1.x;
        };
    };
};
